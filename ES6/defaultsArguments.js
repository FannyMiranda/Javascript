// ## Challenge

// Al igual que en el ejercicio de parámetros de descanso, su objetivo es escribir un módulo de nodo cuya exportación predeterminada sea una función. Esta vez tomará dos argumentos: un límite inferior y un límite superior. Su función debe devolver el punto medio entre esos dos límites.

// Sin embargo, su función debe tener valores predeterminados razonables. El límite inferior debe ser predeterminado a 0, y el límite superior debe ser predeterminado a 1.

// Como antes, no necesita imprimir nada en la consola. Su solución debe verse más o menos así:

//      module.exports = function midpoint (/ * lo que va aquí * /) {
//          // ¿Qué pasa aquí?
//      };

// Probaremos su módulo pasando algunos conjuntos diferentes de argumentos: a veces ninguno, a veces uno, a veces dos. A veces lo superaremos en un undefined explícito, y debe asegurarse de tratarlo como el predeterminado.